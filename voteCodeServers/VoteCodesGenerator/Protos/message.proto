syntax = "proto3";

option csharp_namespace = "GrpcChain";

service ChainService {
  rpc StreamMessages (stream MessageRequest) returns (MessageReply);
}

message MessageRequest {
  string text = 1;
}

message MessageReply {
  string response = 1;
}